packages:
  - package: metaplane/dbt_expectations
    # Use a version compatible with your dbt version. Check dbt Hub for the latest:
    # https://hub.getdbt.com/metaplane/dbt_expectations/versions/
    version: [">=0.10.0", "<0.11.0"]
  - package: dbt-labs/dbt_utils
    # Use a version compatible with your dbt version. For example, for dbt 1.x:
    # https://hub.getdbt.com/dbt-labs/dbt_utils/versions/
    version: [">=1.0.0", "<2.0.0"]
